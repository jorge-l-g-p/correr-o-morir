# Copia este archivo como .env y define valores seguros
# NUNCA uses este archivo directamente en producción

# Contraseña de PostgreSQL — usa una contraseña fuerte y única
DB_PASSWORD=cambia_esto_por_una_clave_segura

# URL del frontend (en producción pon tu dominio)
FRONTEND_URL=http://localhost
