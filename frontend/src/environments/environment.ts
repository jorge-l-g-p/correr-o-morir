/**
 * Variables de entorno para desarrollo.
 * En producción se reemplaza por environment.prod.ts via Angular build.
 */
export const environment = {
  production: false,
  apiUrl:     'http://localhost:3000'
};
